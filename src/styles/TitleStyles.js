import {makeStyles} from '@material-ui/styles';
import './fonts.css'

const TitleStyles = makeStyles(()=>({
    titleStyle: {
        textAlign: 'center',
        fontSize: '3rem',
        fontFamily: 'Cinzel',
    }
}))

export default TitleStyles;